import Styled from "styled-components";

const MainWrapper = Styled.View`
display: flex;
flex-direction: row;
padding: 16px;
justify-content: space-between;
background: grey;
margin-top: 16px;
`;

export { MainWrapper };
